"""The glob module.

Policy: Results are sorted so two runs agree.
"""


def glob_describe() -> str:
    """Return the fact this module is responsible for."""
    return 'Glob expands a pattern against paths, not contents.'
